# dtviewer

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run 
```

To execute the parser test suite:

```bash
bun test
```

This project was created using `bun init` in bun v1.2.24. [Bun](https://bun.com) is a fast all-in-one JavaScript runtime.
